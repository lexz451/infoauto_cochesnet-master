(function () {
    'use strict';

    angular
        .module('app.clients',
            [

            ]
        )
        .config(config);

    /** @ngInject */
    function config($stateProvider, msApiProvider) {}
})();
