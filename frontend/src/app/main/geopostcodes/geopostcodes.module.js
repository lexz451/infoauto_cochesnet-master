(function () {
    'use strict';

    angular
        .module('app.geopostcodes',
            [

            ]
        )
        .config(config);

    /** @ngInject */
    function config($stateProvider, msApiProvider) {}
})();
