(function () {
    'use strict';

    angular
        .module('app.tasks',
            [

            ]
        )
        .config(config);

    /** @ngInject */
    function config($stateProvider) {

    }
})();
